// Write a program to print a given number without its last digit.

#include<stdio.h>
#include<conio.h>

    void main()
{
        int a;
        printf("Enter number ");
        scanf("%d",&a);
        printf("Given number without its last digit %d",a/10);
        
    getch();
} 
